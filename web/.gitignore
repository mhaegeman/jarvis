node_modules
dist
.vite
.env.local
playwright-report
test-results

# Local staticrypt encrypt cache — leaks the password salt+hash; deploy workflow regenerates this.
.staticrypt.json

# TypeScript composite-build artifacts for config files
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts

# Re-include package files (root .gitignore globally excludes "package*")
!package.json
!package-lock.json
